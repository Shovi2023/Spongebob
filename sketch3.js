let ele;
function setup() {
  ele = createAudio('project2/karenvoice.mp3');

  // here we set the element to autoplay
  // The element will play as soon
  // as it is able to do so.
  ele.autoplay(true);
}

